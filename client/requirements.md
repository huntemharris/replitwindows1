## Packages
date-fns | Date manipulation for calendar and booking logic
react-day-picker | Calendar component for selecting booking dates
lucide-react | Icons for UI elements (already in base but explicit check)
framer-motion | For smooth step transitions in the wizard

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
